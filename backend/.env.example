# Server
PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:8080

# MongoDB
MONGO_URI=mongodb://localhost:27017/messmate

# JWT secrets (use openssl rand -hex 32 in production)
JWT_ACCESS_SECRET=change_me_access_secret_min_32_chars_long
JWT_REFRESH_SECRET=change_me_refresh_secret_min_32_chars_long
JWT_QR_SECRET=change_me_qr_secret_min_32_chars_long

# Token TTLs
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
QR_TOKEN_TTL_SECONDS=8

# Notifications (optional — leave blank to disable)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_WHATSAPP_FROM=
